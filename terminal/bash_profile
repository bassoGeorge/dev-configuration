# Just part of the bash_profile, which is cross platform compatible between mac and linux
# Customise the prompt for special awesomeness
PS1="\u@\h:[\W]>\$ "
PS2="\-> "

# vi(m) forever
export EDITOR="vim"
set -o vi         # The command line behaves in vi mode
